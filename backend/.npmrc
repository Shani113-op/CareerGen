# NPM configuration for CareerGenAI Python Backend
# This backend uses Python, not Node.js dependencies

# Skip optional dependencies since we don't need them
optional=false

# Disable package-lock for this Python project
package-lock=false